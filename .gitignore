node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.local

# Probe + runtime artefacts. These contain account-identifying metadata
# (user UUIDs, filenames, album titles, camera serials, location) and this
# repo is public — never commit them. Sanitised fixtures may be added by hand.
docs/probe-results*.json
docs/probe-results*.md
__pycache__/
*.pyc
*.sqlite
*.sqlite-journal

# Never commit credentials
probe_token
*.token
